# Persistenz / lokale Daten — niemals committen (enthaelt Passwort-Hashes)
db.json
db.test.json
*.tmp

# Umgebungsvariablen / Secrets — niemals committen
.env

# macOS / Editor
.DS_Store

# Node
node_modules/
*.log
